# Training / distillation extras (the CLEANING pipeline needs only CLEANING/requirements.txt)
# Laptop CPU:   pip install -r TRAINING/requirements-train.txt
# Colab GPU:    the notebook installs the same list (torch already present there)
requests>=2.31
numpy>=1.24,<3
scikit-learn>=1.3
joblib>=1.3
datasets>=2.18
huggingface_hub>=0.23
torch>=2.1            # CPU wheel is fine on the laptop: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40,<5
sentence-transformers>=3.0
setfit>=1.1
accelerate>=0.30
gliner>=0.2.13
sentencepiece>=0.2
protobuf>=4.25
